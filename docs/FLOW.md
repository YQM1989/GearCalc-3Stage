# GearCalc-3Stage Flow

## 运行时数据流

```mermaid
flowchart LR
    A["用户输入电机与级数"] --> B["React 状态 GearCalcInput"]
    B --> C["截取启用的 1–3 级"]
    C --> D["输入校验"]
    C --> E["逐级速比、转速、扭矩、效率"]
    E --> F["总速比、总效率、输出方向"]
    C --> G["中心距与齿轮几何"]
    D --> H["输入错误"]
    E --> I["工程风险规则"]
    F --> J["结果总览"]
    G --> K["逐级表与尺寸表"]
    H --> L["校验与风险提示"]
    I --> L
    B --> M["齿轮视觉预览"]
```

所有计算发生在前端内存中。当前没有网络请求、数据库、文件写入或 Tauri IPC。

## 逐级计算顺序

1. 将级数约束到 1–3；
2. 只取 `stages` 中启用的前 N 级；
3. 校验电机、压力角、模数、齿数和效率；
4. 无效齿数归零，不继续形成有效齿轮副；
5. 计算本级速比；
6. 用上一级输出作为下一级输入；
7. 累乘总速比和总效率；
8. 计算中心距和两只齿轮的基础尺寸；
9. 生成输入错误与工程风险；
10. React 同步刷新总览、预览、表格和提示。

## Web 工程闭环

```mermaid
flowchart TD
    A["Node 24 LTS"] --> B["npm ci"]
    B --> C["npm test"]
    C --> D["TypeScript 检查"]
    D --> E["Vite 生产构建"]
    E --> F["本地页面启动"]
    F --> G["默认值、示例、非法输入交互验收"]
```

通过条件：

- npm 审计无已知漏洞；
- 计算测试全部通过；
- TypeScript 和 Vite 构建通过；
- 默认参数与独立公式一致；
- 两级真实参数与独立公式一致；
- 非整数齿数显示明确错误并停止有效输出；
- 浏览器控制台无错误。

## macOS Tauri 工程闭环

```mermaid
flowchart TD
    A["Xcode Command Line Tools"] --> C["rustup stable arm64"]
    B["Node 24 LTS + npm"] --> D["前端构建"]
    C --> E["cargo fmt/check/clippy"]
    D --> F["Tauri release"]
    E --> F
    F --> G["生成 arm64 .app"]
    G --> H["本机 ad-hoc 签名"]
    H --> I["生成并校验 DMG"]
    I --> J["启动桌面窗口并操作示例"]
```

本机工程验收与公开发布必须分开：

- 本机验收：ad-hoc 签名即可验证应用结构、启动和交互；
- 公开发布：需要 Apple Developer ID、正式签名、公证、stapling 和另一台 Mac 的 Gatekeeper 验证；
- 仓库中不得保存证书、私钥、账号密码或公证凭据。

## 回归验收参数

推荐保留以下两组：

1. 默认一级：1500 rpm、0.2 N·m、20→40 齿、95%，期望输出 750 rpm、0.38 N·m；
2. 两级解释样例：1450 rpm、0.5 N·m，20→60 齿/95%，18→72 齿/94%，期望总速比 12、总效率 89.3%、输出 120.83 rpm 和 5.358 N·m。

仓库内置三级示例主要用于同时验证多级计算、低齿数提示和三级累计风险。
